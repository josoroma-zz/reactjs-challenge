import React from "react";
import { ContentMessage } from "components";

const Progress = () => (
  <div data-testid="id-request-progress">
    <ContentMessage type="progress" />
  </div>
);

export default Progress;
